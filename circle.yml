machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker pull insighttoolkit/simpleitk-notebooks:2015-miccai

test:
  override:
    - cd ~/SimpleITKTutorialSPIE2016 && python ./downloaddata.py ./Data ./Data/manifest.json
    - docker run -v ~/SimpleITKTutorialSPIE2015:/home/jovyan/notebooks insighttoolkit/simpleitk-notebooks:2015-miccai ./test.sh
